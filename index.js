function shout(string)  {
  return string.toUpperCase()
}
//shout takes argument "string" and returns it in all uppercase
//shout (hello!) > "HELLO!"

function whisper(string) {
  return string.toLowerCase()
}
//whisper takes argument "string" and returns it in all lowercase
//whisper (HELLO!) > hello!
